{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "remove containers",
            "type": "shell",
            "command": "docker rm -f $(docker ps -a -q)",
            "problemMatcher": []
        },
        {
            "label": "prepare to debug",
            "type": "shell",
            "command": "docker run --privileged -v ${workspaceFolder}:/usr/local/src/sd_socle --name sd-socle-europe-test_container -it sd-socle-europe-test ",
            "group": "build",
            "problemMatcher": []
        }
    ]
}